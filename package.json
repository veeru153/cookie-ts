{
  "name": "cookie-ts",
  "version": "0.0.1",
  "description": "Cookie Bot for Yuqicord",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prestart": "yarn build",
    "start": "node -r dotenv/config .",
    "dev": "nodemon -L --ext ts --exec \"NODE_ENV=dev yarn start\""
  },
  "author": "veeru153",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.3.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "firebase-admin": "^11.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
