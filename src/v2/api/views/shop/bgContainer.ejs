<div class="container" onclick="navigator.clipboard.writeText('-shop buy <%= item.id %>')">
    <img src="<%= item.src %>" alt="<%= item.name %>" />
    <div class="info">
        <p>
            <%= item.name %>
        </p>
        <p>
            <%= item.cost %> 🍪
        </p>
    </div>
</div>